<?xml version="1.0" encoding="UTF-8"?>
<!--
  Maven build for the boom-bot MMO game service.

  Note: the canonical, toolchain-only build is `./build.sh` (javac + jar +
  a vendored sqlite-jdbc driver) so the service compiles offline with just a
  JDK. This pom is provided for teams that prefer Maven; the maven-jar-plugin
  rescans the same sources so both paths produce the same game-service jar.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.boombot</groupId>
    <artifactId>mmo-server</artifactId>
    <version>0.1.0</version>
    <packaging>jar</packaging>

    <name>boom-bot MMO Game Service</name>
    <description>Persistent, event-sourced MMO game service backed by the shared wallet.</description>

    <properties>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <sqlite.jdbc.version>3.43.0.0</sqlite.jdbc.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>${sqlite.jdbc.version}</version>
        </dependency>
    </dependencies>

    <build>
        <finalName>mmo-server</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.1</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>com.boombot.mmo.MmoServerMain</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>